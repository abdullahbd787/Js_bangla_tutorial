var arr = [1, 2, 3, 4, 5, 6, 7, 8];

// arr[3] = 9; //Insert  9 to index 3
// arr.push(9); //insert element in the array of last
// arr.unshift(9); //insert element in the array of fast
// arr.splice(3, 0, 9, 10); //insert element in index 3, 9,10 inserted
// arr[3] = undefined; // index 3 velue undefined
// arr.pop(); // remove last element
// arr.shift(); //remove first element
// arr.splice(3, 1,);//remove data of index 3
arr.splice(3, 1, 44); //remove data of index 3 nirdisto data delet and add or replace used splice method

console.log(arr);
