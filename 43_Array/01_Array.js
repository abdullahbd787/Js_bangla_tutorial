var arr = [1, 2, 3, 4, 5];
arr[5] = 6; //create new array index
arr[7] = 8; //create new array index
arr[3] = 16; // replace index of item
console.log(arr);
console.log(arr.length); //arrays items of size

var arr2 = Array(); // create a empty array
console.log(arr2);
