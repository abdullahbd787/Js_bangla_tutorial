var arr = [1, 2, 3, 4, 5];

var sum = 0; //store of all sumesion.

for (var i = 0; i < arr.length; i++) {
  sum += arr[i]; // sum = sum + arr[i]
}
console.log(sum);

sumsss = 0;
for (var i = 0; i < arr.length; i++) {
  if (arr[i] % 2 === 0) {
    console.log(arr[i]);
  }
}
